/**
 * 1. create firebase project
 * 2. create web app
 * 3. create firebase.config.js and export default app
 * 4. Build > Authentication > Get Started > Enable Sign In Method
 * */ 

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * */ 

/**
 * HOSTING
 * ----------------
 * Per pc one time
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For Each Project One Time
 * 1. firebase init
 * 2. process
 * 3. hosting: firebase (up and down arrow) and space bar to select
 * 4. existing project
 * 5. select the project careful
 * 6. which folder of project as public directory: dist (if you are using vite) // which folder you will use for deploy
 * 7. single page application: yes
 * 8. continuos deployment: no
 * 
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 * 
 * */ 